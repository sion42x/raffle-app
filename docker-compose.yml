services:
  raffle:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - raffle-data:/app/data
    environment:
      - DB_PATH=/app/data/raffle.db
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped

volumes:
  raffle-data:
